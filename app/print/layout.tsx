import "../globals.css";
import "./print.css";

export const metadata = {
  title: "Alexi Claasen — CV Print",
};

export default function PrintLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
